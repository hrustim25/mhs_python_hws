
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mhs_python_hws"

readme = "README.md"
authors = [
    { name = "Rustem Gimadiev", email = "hrustim25@gmail.com" },
]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.8"

# No dependencies
dependencies = []

[project.urls]
"Homepage" = "https://github.com/hrustim25/mhs_python_hws"
